import { describe, test } from 'vitest';

describe('blob', () => {
  test('blob to text', () => {
    //
  });

  test('blob to json', () => {
    //
  });

  test('blob to xml', () => {
    //
  });

  test('blob to video', () => {
    //
  });

  test('blob to (auto detect)', () => {
    //
  });
});
